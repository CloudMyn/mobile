import 'package:flutter/material.dart';

class SupervisorApprovalsTab extends StatelessWidget {
  const SupervisorApprovalsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Daftar persetujuan atasan (Tahap Pengembangan)'),
    );
  }
}
